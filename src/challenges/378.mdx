### [Kth Smallest Element in a Sorted Matrix](https://leetcode.com/problems/kth-smallest-element-in-a-sorted-matrix/)

- Use binary search on value range (not indices)
- Count elements ≤ mid value
- If count ≥ k, search left half; else search right
- Start from bottom-left for efficient counting

#### Visual Explanation

**Binary Search on Values:**
```
Matrix:          Range: [1, 13]
[1,  5,  9]      
[10, 11, 13]     mid = 7
[12, 13, 15]     
                 Count ≤ 7: 
k = 8            Row 0: 1,5 (2)
                 Row 1: none (0)
                 Row 2: none (0)
                 Total: 2 < 8
                 Search right half

Continue until found...
```

📺 [Watch Visual Explanation](https://www.youtube.com/watch?v=o-YDQzHoaVU)

```js copy
/**
 * @param {number[][]} matrix
 * @param {number} k
 * @return {number}
 */
var kthSmallest = function(matrix, k) {
    const n = matrix.length;
    let left = matrix[0][0];
    let right = matrix[n - 1][n - 1];
    
    // Count elements <= mid
    function countLessEqual(mid) {
        let count = 0;
        let col = n - 1;
        
        // Start from bottom-left
        for (let row = 0; row < n; row++) {
            while (col >= 0 && matrix[row][col] > mid) {
                col--;
            }
            count += col + 1;
        }
        
        return count;
    }
    
    while (left < right) {
        const mid = left + Math.floor((right - left) / 2);
        const count = countLessEqual(mid);
        
        if (count < k) {
            left = mid + 1;
        } else {
            right = mid;
        }
    }
    
    return left;
};
```

##### Complexity

- Time: O(n × log(max - min)) where n is matrix dimension
- Space: O(1)
### [Invert Binary Tree](https://leetcode.com/problems/invert-binary-tree/)

- Use recursion to swap left and right subtrees
- Base case: return null for null nodes
- Recursively invert left and right subtrees
- Swap the subtrees and return root

#### Visual Explanation

**Tree Inversion (Mirror):**
```
Original:       4
               / \
              2   7
             / \ / \
            1  3 6  9

Step-by-step inversion:

At node 4:
  Swap left(2) and right(7)
  Recurse on both subtrees

At node 2:
  Swap left(1) and right(3)
  
At node 7:
  Swap left(6) and right(9)

Inverted:       4
               / \
              7   2
             / \ / \
            9  6 3  1

Visual transformation:
    4              4
   / \            / \
  2   7    →     7   2
 / \ / \        / \ / \
1  3 6  9      9  6 3  1
```

**Key insight:**
```
For each node:
  temp = left
  left = invert(right)
  right = invert(temp)
```

📺 [Watch Visual Explanation](https://www.youtube.com/watch?v=OnSn2XEQ4MY)

```js copy
/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {TreeNode}
 */
var invertTree = function(root) {
    if (!root) return null;
    
    // Swap left and right
    const temp = root.left;
    root.left = invertTree(root.right);
    root.right = invertTree(temp);
    
    return root;
};
```

##### Complexity

- Time: O(n) where n is number of nodes
- Space: O(h) where h is height (recursion stack)